export const PUBLIC_ROUTES = {
  SIGN_IN: 'sign-in'
};

export const PRIVATE_ROUTES = {
  DASHBOARD: { ROOT: '/dashboard' },
  MODULE: { ROOT: '/module', DETAIL: '/module/:id' },
  MANAGEMENT: { ROOT: '/management' }
};
